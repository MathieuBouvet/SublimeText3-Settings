{
	"shell_cmd": "projectGenerator ${file_extension} ${file_path} && cd .. && make && gnome-terminal -e 'bash -c \"${file_path}/../bin/run && echo && echo Press ENTER to continue && read line && exit\"'",
	"file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
	"selector": "source.c++, source.cpp, source.c",
}
